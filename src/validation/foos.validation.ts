//import joi from 'joi'

export const foosValidation = {
    // query: joi.object().keys({
    //     foo: joi.string().required(),
    // }),
    // body: joi.object().keys({
    //     bar: joi.string().allow(['foo', 'bar']).only(),
    // }),
}
